'use babel';

import GololangWebcomponentsView from '../lib/gololang-webcomponents-view';

describe('GololangWebcomponentsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
